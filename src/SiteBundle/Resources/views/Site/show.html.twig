{% extends 'base.html.twig' %}


{% block body %}

 

<div class="row mt-3">
  <div class="col-md-8 mx-auto">
      
      <div class="text-right">
           <a href="/site" class="btn btn-link">
              <i class="fa fa-chevron-cercle-left"></i> Back to list
           </a>
      </div>

        <h1>{{ myPost['title'] }}</h1>
        <img src="{{ myPost['image'] }}" alt="{{ myPost['title'] }}">
        <p>{{ myPost['content'] }}</p>
  </div>

</div>


{% endblock %}